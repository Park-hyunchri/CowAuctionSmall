# CowAuctionSmall Codex Handoff

## 1. 프로젝트 목적

CowAuctionSmall은 가축 경매 현장에서 사용하는 Windows 기반 경매/전광판 클라이언트 프로그램이다.

경매 서버와 통신하여 출품우 정보, 경매 상태, 낙찰/유찰 결과 등을 수신하고 전광판 화면에 표시한다.

현장 운영 프로그램이므로 기존 동작 안정성과 축협별 요구사항 호환성을 최우선으로 한다.

---

## 2. 기술 구성

- C#
- WPF
- MVVM
- .NET 9.0
- Windows
- Git / GitHub

주요 폴더:

- `Models`
- `ViewModels`
- `Views`
- `Services`
- `NetProto`
- `Config`
- `Utils`
- `Resources`

---

## 3. Codex 작업 규칙

작업 전 반드시 루트의 `AGENTS.md`를 먼저 읽는다.

가장 중요한 원칙:

사용자가 명시적으로 **"수정 승인"**이라고 말하기 전까지 어떠한 소스 파일도 생성, 수정, 삭제하지 않는다.

기본 동작은 분석 전용이다.

문제가 발견되면 먼저 다음을 보고한다.

1. 문제의 원인
2. 관련 소스 파일
3. 수정이 필요한 위치
4. 제안하는 수정 내용
5. 기존 기능에 미칠 수 있는 영향
6. 수정 후 검증 방법

사용자의 명시적인 **"수정 승인"** 이후에만 실제 수정 작업을 진행한다.

---

## 4. 현재 Git 상태

현재 기준:

- Branch: `master`
- Remote: `origin/master`
- 최신 확인 커밋: `e8b868c`
- 회사 PC와 GitHub 동기화 완료
- Working tree clean

최근 커밋:

- `e8b868c` Codex 작업 규칙 추가
- `6c8f92b` 유찰 뷰 페이지 정리
- `b5d94ac` 뷰 페이지 정리 및 users.xml 옵션 적용

작업 시작 시 반드시 다시 확인한다.

```powershell
git status
git branch -vv
git log --oneline --decorate -5
```

---

## 5. 프로젝트 참고 문서

### `AGENTS.md`

Codex가 반드시 따라야 하는 작업 규칙.

주요 내용:

- 수정 승인 전 분석만 수행
- 승인된 파일만 수정
- 대규모 리팩터링 금지
- 승인 없는 Git 작업 금지
- 수정 후 빌드/테스트/변경 파일 보고

---

### `기록용.txt`

과거 프로그램 변경 내용과 현장 장애 대응 기록.

주요 기록 예:

- 메모리 급증 문제
- 서버 재연결 처리
- SV 코드 처리 개선
- SS 코드 기반 연결 상태 판단
- 경매 화면 오류 수정
- 축협별 커스터마이징
- 애니메이션 최적화
- .NET 버전 변경

과거 문제를 조사할 때 참고한다.

단, 기록 내용이 현재 소스와 동일하다고 가정하지 말고 반드시 실제 소스를 확인한다.

---

### `error.md`

현재 CowAuctionSmall 프로그램과 직접 관련 없는 LTE / 라이다 / 센서 / 임베디드 장비 로그가 포함되어 있다.

따라서 CowAuctionSmall 문제 분석 시 기본 판단 근거로 사용하지 않는다.

사용자가 해당 로그 분석을 명시적으로 요청한 경우에만 참고한다.

---

## 6. 기존 프로그램의 주요 특징

이 프로그램은 실제 가축 경매 현장에서 사용되며, 다음 기능이 포함되어 있다.

- 경매 서버 통신
- 출품우 데이터 수신
- 경매 상태 처리
- 낙찰 / 유찰 화면 표시
- 개체정보 표시
- 서버 연결 상태 확인 및 재연결
- 축협별 화면 커스터마이징
- 경매 종류 및 전광판 해상도별 화면 처리
- 애니메이션 및 UI 상태 처리

축협별 별도 요구사항이 코드에 포함되어 있을 수 있으므로 공통 로직 수정 시 다른 지역 동작에 미치는 영향을 반드시 확인한다.

---

## 7. 과거 주요 문제

과거 기록상 다음 문제가 있었음.

### 메모리 증가

과거 ClosedXML 사용으로 메모리가 지속적으로 증가하여 프로그램이 중단되는 문제가 있었다.

또한 서버 통신 이상 상황에서 메모리 사용량이 급증한 기록이 있다.

현재 동일 문제가 존재한다고 단정하지 말고 실제 코드와 실행 상태를 확인한다.

---

### 서버 연결 상태

서버와 실제 연결이 끊겼는데 연결된 것으로 판단하는 문제가 있었음.

과거 SS 코드 수신 여부를 기준으로 서버 연결 상태를 판단하고 프로그램을 재시작하는 로직이 적용되었다.

네트워크 관련 코드 수정 시 기존 재연결 및 SS 처리 흐름을 반드시 분석한다.

---

### SV 코드 처리

출품우 변경 또는 새로고침 시 SV 코드가 대량으로 발생하면서 UI 및 서버 부하가 증가하는 문제가 있었다.

과거 일정 시간 동안 데이터를 모은 뒤 UI를 갱신하도록 개선된 기록이 있다.

관련 코드 수정 시 단순 이벤트 단위 처리로 되돌리지 않도록 주의한다.

---

### 축협별 커스터마이징

다음과 같은 지역별 요구사항이 과거에 적용되어 있음.

예:

- 예천
- 강진완도
- 무진장
- 정읍
- 기타 축협

따라서 화면 또는 데이터 표시 로직을 수정할 때 특정 축협 조건문 및 설정값을 반드시 확인한다.

---

## 8. 작업 시작 절차

Codex는 매 작업 시작 시 다음 순서를 따른다.

1. `AGENTS.md` 읽기
2. `CODEX_HANDOFF.md` 읽기
3. `git status` 확인
4. `git log --oneline --decorate -5` 확인
5. 사용자 요청 내용 확인
6. 관련 소스 파일 분석
7. 문제 원인 및 수정 방안 보고
8. 사용자의 **"수정 승인"** 대기

승인 전에는 실제 소스 수정 금지.

---

## 9. 수정 작업 시 원칙

1. 기존 WPF / MVVM 구조를 최대한 유지한다.
2. 현재 동작을 먼저 이해한 후 수정한다.
3. 한 번에 대규모 리팩터링하지 않는다.
4. 승인받은 파일만 수정한다.
5. 축협별 커스터마이징 영향을 확인한다.
6. 네트워크 로직 수정 시 기존 프로토콜을 확인한다.
7. UI 수정 시 다른 해상도 및 화면 상태 영향을 확인한다.
8. 불확실한 부분은 추측하지 않는다.
9. 설정값이나 운영값을 임의로 변경하지 않는다.
10. `.csproj`, NuGet 패키지, Git 설정은 별도 승인 없이 변경하지 않는다.

---

## 10. 수정 완료 후 보고

수정 작업 완료 후 반드시 다음 내용을 보고한다.

1. 수정한 파일 목록
2. 파일별 변경 내용
3. 수정 이유
4. 기존 기능에 미치는 영향
5. 빌드 결과
6. 테스트 결과
7. 추가 확인이 필요한 사항
8. `git diff` 기준 최종 변경 요약

Git commit / push는 사용자의 별도 승인 이후에만 진행한다.

---

## 11. 회사 / 집 PC 작업 흐름

### 회사 PC에서 작업 시작

```powershell
git pull
git status
git log --oneline --decorate -5
```

그 다음:

- `AGENTS.md` 확인
- `CODEX_HANDOFF.md` 확인
- 작업 시작

---

### 회사 PC에서 작업 종료

1. 수정 내용 확인
2. 빌드 및 테스트
3. `git diff` 확인
4. `CODEX_HANDOFF.md` 업데이트
5. 사용자 승인
6. commit
7. push

---

### 집 노트북에서 작업 시작

```powershell
git pull
git status
git log --oneline --decorate -5
```

Codex에게 다음과 같이 요청한다.

> AGENTS.md와 CODEX_HANDOFF.md를 먼저 읽고 현재 Git 상태를 확인해.  
> 아직 파일을 수정하지 말고 현재 작업 상태와 다음 작업 내용을 정리해서 보고해.

---

### 집 노트북에서 작업 종료

1. 수정 내용 확인
2. 빌드 및 테스트
3. `git diff` 확인
4. `CODEX_HANDOFF.md` 업데이트
5. 사용자 승인
6. commit
7. push

---

## 12. Codex에게 기본으로 줄 시작 명령

새 PC 또는 새로운 Codex 세션에서 다음 내용을 사용한다.

> `AGENTS.md`와 `CODEX_HANDOFF.md`를 먼저 읽어.  
> `git status`와 최근 Git 로그를 확인하고 현재 프로젝트 상태를 파악해.  
> `기록용.txt`는 과거 변경 이력 참고용으로만 사용하고 실제 소스를 우선 확인해.  
> `error.md`는 CowAuctionSmall과 직접 관련 없는 장비 로그가 포함되어 있으므로 내가 요청하지 않는 한 분석 대상에서 제외해.  
> 아직 파일을 수정하지 말고 현재 상태와 다음 작업 방향만 보고해.

---

## 13. 현재 다음 단계

현재 회사 PC와 GitHub는 동기화된 상태이다.

다음 실제 작업 요청이 들어오기 전까지 소스를 수정하지 않는다.

새 작업을 시작하면:

1. 사용자 요청 확인
2. 관련 코드 분석
3. 원인 및 수정안 보고
4. 사용자 **"수정 승인"** 이후 실제 수정

이 순서를 유지한다.