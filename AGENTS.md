# CowAuctionSmall 작업 규칙

이 문서는 CowAuctionSmall 저장소에서 작업하는 AI 코딩 도구가 따라야 하는 최우선 프로젝트 규칙이다. 프로젝트에 더 구체적인 하위 `AGENTS.md`가 있더라도, 아래의 안전·승인 규칙은 완화할 수 없다.

## 1. 기본 원칙: 분석 우선, 수정은 승인 후

- 모든 요청은 먼저 읽기 전용으로 분석한다.
- 관련 코드, 호출 흐름, 데이터 바인딩, 기존 구현 방식과 영향 범위를 확인한 뒤 원인과 수정안을 사용자에게 보고한다.
- 사용자가 **명시적으로 수정 승인**하기 전에는 소스·설정·리소스 파일을 생성, 수정, 이동, 이름 변경 또는 삭제하지 않는다.
- “확인해 줘”, “원인을 찾아 줘”, “어떻게 고치면 돼?”, “진행해 봐”처럼 범위가 불명확한 표현은 수정 승인으로 간주하지 않는다.
- 승인 전에는 파일 읽기, 문자열 검색, Git 상태·로그·diff 확인 등 상태를 바꾸지 않는 조사만 수행한다.
- 분석 결과에는 다음 내용을 포함한다.
  - 문제 현상과 재현 조건
  - 추정 원인과 근거
  - 수정이 필요한 파일 목록
  - 파일별 수정 이유와 예상 변경 내용
  - 영향 범위와 위험 요소
  - 빌드·테스트 계획

## 2. 승인 범위

- 사용자가 승인한 **정확한 파일만** 수정한다.
- 승인 요청 시 파일 경로와 수정 목적을 구체적으로 제시한다.
- 승인된 목적을 넘어서는 정리, 포맷 변경, 이름 변경, 코드 이동 또는 “함께 하면 좋은 개선”을 추가하지 않는다.
- 작업 중 다른 파일의 수정이 필요해지면 즉시 멈추고, 필요한 파일과 이유를 보고한 뒤 **추가 승인을 다시 받는다**.
- 승인된 파일이라도 예상보다 변경 범위가 크게 늘어나면 다시 보고하고 재승인을 받는다.
- 실제 작업 승인은 해당 구현에 따른 `WORKLOG.md` 기록까지 포함한 것으로 본다. 단, 작업 기록 외의 문서 변경은 별도 승인을 받아야 한다.

승인의 올바른 예:

> `Services/AnimalParseData.cs`와 `ViewModels/RunningViewModel.cs` 수정 승인. 제안한 친자일치 표시 처리만 진행해 주세요.

승인으로 보지 않는 예:

> 한번 봐 주세요.

## 3. 절대 금지 작업

다음 작업은 일반적인 수정 승인에 포함되지 않으며 수행하지 않는다. 필요성이 발견되면 실행하지 말고 사용자에게 이유만 보고한다.

- 대규모 리팩터링, 폴더 구조 개편, 광범위한 파일 이동 또는 일괄 포맷 변경
- NuGet 등 패키지 추가·삭제·업데이트 및 패키지 복원(restore)
- `.csproj`, `.sln`, `Directory.Build.*`, 패키지 잠금 파일 수정
- Git `commit`, `push`, 강제 push, 태그 생성·삭제
- 브랜치 생성·전환·병합·리베이스·삭제
- Git `reset`, `clean`, `restore`, `checkout`을 이용한 변경 폐기
- 사용자 변경 사항 덮어쓰기 또는 되돌리기
- `bin`, `obj`, `.vs`, `.git` 내부 파일의 직접 생성·수정·이동·삭제

빌드나 테스트가 `bin` 또는 `obj`에 자동 생성물을 만드는 것은 승인된 검증 과정에서만 허용되는 예외다. 해당 폴더를 직접 편집하거나 정리하지 않으며 Git 변경 대상으로 취급하지 않는다.

## 4. 구현 규칙

- 기존 C# 버전, .NET/WPF 대상 프레임워크, MVVM 구조, 네이밍, 들여쓰기, 파일 인코딩과 줄바꿈 방식을 유지한다.
- View(XAML), ViewModel, Model, Service의 기존 책임 경계를 지킨다.
- 화면 표시 문제는 XAML 바인딩, `DataContext`, Converter, `INotifyPropertyChanged`, Command 흐름을 함께 확인한다.
- UI 스레드 접근, `Dispatcher`, 비동기 처리, 타이머, 애니메이션 및 `Loaded`/`Unloaded` 이벤트의 수명 주기를 점검한다.
- 기존 구조상 불가피한 경우가 아니면 code-behind에 새로운 업무 로직을 넣지 않는다.
- 경매 진행·낙찰·유찰 상태, 출품 번호, 축산 개체 정보, 사업장별 표시 규칙, 전광판 해상도별 View를 변경할 때 다른 상태·사업장·해상도에 미치는 영향을 확인한다.
- 통신·파싱 코드는 누락값, 빈 문자열, 잘못된 형식, 연결 끊김 및 중복 수신을 고려하되 승인 범위 밖의 방어 코드를 임의로 추가하지 않는다.
- 비밀번호, API 키, 접속 문자열, 개인정보 또는 운영 서버 정보를 코드·로그·작업 기록에 새로 남기지 않는다.
- 관련 없는 코드 정리, 주석 수정, using 정렬 또는 전체 파일 포맷 변경을 하지 않는다.

## 5. 빌드 및 테스트

- 구현 완료 후 승인 범위에 맞는 최소 단위의 빌드와 테스트를 수행한다.
- 패키지 복원, clean 또는 프로젝트 파일 변경 없이 검증 가능한 명령만 사용한다. 가능한 경우 `--no-restore` 또는 이에 해당하는 옵션을 사용한다.
- 전체 솔루션 빌드가 불가능하면 실패 원인과 확인하지 못한 범위를 숨기지 않고 보고한다.
- 자동화 테스트가 없다면 관련 화면과 상태의 수동 확인 항목을 구체적으로 제시한다.
- WPF 전광판 변경 시 가능한 범위에서 다음을 확인한다.
  - 경매 진행·낙찰·유찰 화면
  - 주요 전광판 해상도와 화면 전환
  - 데이터 바인딩 갱신과 빈 값 처리
  - 사업장별 조건 분기
  - 타이머·애니메이션·이벤트 해제와 반복 진입
  - 기존 통신 및 파싱 흐름의 회귀 여부

## 6. 작업 완료 보고

수정 후에는 사용자에게 다음을 빠짐없이 보고한다.

- 실제 변경 파일 목록
- 파일별 변경 내용과 변경 이유
- 승인 범위와 실제 변경 범위의 일치 여부
- 빌드 명령과 성공·실패 결과
- 수행한 테스트와 결과, 수행하지 못한 테스트
- `git status`와 `git diff`로 확인한 변경 요약
- 남은 위험, 추가 확인 사항 또는 수동 테스트 항목
- 제안하는 Git commit 메시지

Git diff를 확인할 때 사용자에게 이미 있던 변경을 내 작업으로 보고하지 않는다. 예상하지 못한 변경을 발견하면 중단하고 사용자에게 알린다.

## 7. WORKLOG.md 기록

- 사용자가 승인한 실제 작업이 구현과 검증까지 완료되면 프로젝트 루트의 `WORKLOG.md`에 작업 내역을 추가한다.
- 분석만 수행했거나 구현이 완료되지 않은 작업은 기록하지 않는다.
- 기존 기록은 삭제하거나 덮어쓰지 않고, **가장 최신 기록을 파일 위쪽에** 추가한다.
- `WORKLOG.md`의 모든 필수 항목을 작성하고, 확인하지 못한 값은 추측하지 말고 `미확인`, `미수행` 또는 `해당 없음`으로 표시한다.
- Git commit과 push는 직접 수행하지 않는다. commit 전에는 hash를 `미생성`, push 여부를 `미수행`으로 기록하고 commit 메시지만 제안한다.
- 업무 정보, 개인정보, 인증 정보, 운영 접속 정보와 불필요하게 긴 로그를 기록하지 않는다.

## 8. Git 작업 규칙

Git Commit 및 Push는 사용자의 명시적인 요청이 있을 때만 수행한다.

허용되는 요청 예시

- 커밋
- Commit
- 커밋해줘
- 커밋 및 푸시
- Commit & Push

Commit 수행 전에는 반드시

1. git status
2. 변경 파일 목록
3. Commit Message

를 확인한다.

Commit Message를 사용자가 지정한 경우
해당 메시지를 그대로 사용한다.

사용자가 지정하지 않은 경우
Codex가 적절한 메시지를 제안한다.

Commit 성공 후 Push를 수행한다.

Push 실패 시 원인을 보고한다.

사용자가 Git 관련 작업을 취소하면
Commit과 Push를 수행하지 않는다.