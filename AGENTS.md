# CowAuctionSmall Codex 작업 규칙

## 기본 원칙

이 프로젝트는 실제 가축 경매 현장에서 사용하는 운영 프로그램이다.

사용자가 명시적으로 "수정 승인"이라고 말하기 전까지
어떠한 소스 파일도 생성, 수정, 삭제하지 않는다.

기본 동작은 분석 전용이다.

## 수정 전 절차

문제가 발견되면 먼저 다음 내용을 사용자에게 보고한다.

1. 문제의 원인
2. 관련 소스 파일
3. 수정이 필요한 위치
4. 제안하는 수정 내용
5. 기존 기능에 미칠 수 있는 영향
6. 수정 후 검증 방법

보고한 뒤 작업을 중지하고 사용자 승인을 기다린다.

"수정 승인"이라는 명시적인 지시가 있을 때만
실제 파일을 수정한다.

## 수정 범위

승인된 파일만 수정한다.

승인되지 않은 파일을 추가로 수정해야 하는 경우
먼저 이유를 설명하고 다시 승인을 받는다.

대규모 리팩터링을 임의로 수행하지 않는다.

기존 WPF / MVVM 구조와 현재 프로젝트 설계를 최대한 유지한다.

## 금지 사항

사용자 별도 승인 없이 다음 작업을 하지 않는다.

- 파일 삭제
- 프로젝트 전체 구조 변경
- NuGet 패키지 업데이트
- .csproj 변경
- Git commit
- Git push
- 브랜치 삭제
- git reset --hard
- git clean
- git restore를 이용한 기존 작업 삭제
- 설정 파일의 운영값 변경

bin, obj, .vs, .git 폴더의 파일은 수정하지 않는다.

## 수정 후 보고

수정을 완료하면 반드시 다음을 보고한다.

1. 수정한 파일 목록
2. 각 파일에서 변경한 내용
3. 수정 이유
4. 빌드 결과
5. 테스트 결과
6. 추가 확인이 필요한 사항

가능하면 git diff를 기준으로 변경 내용을 다시 검토한다.